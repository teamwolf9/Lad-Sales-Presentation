node_modules/
dist/
*.local
.DS_Store

# Build artifacts emitted by tsc -b / vite
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Firebase
.env
.env.local
.env.*.local
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
extensions/*.secret.local
extensions/*.env.local
extensions/*.env

# Raw design-system drop + source archive (reference only, large binaries)
/design-system/
*.zip

# Raw employee headshot originals — slugified copies live in public/employees
/Employees/

# Playwright MCP — persisted Chromium profile (auth cookies, localStorage).
# Per-developer; never commit. See claude-browser-automation-setup.
.playwright-profile/

# Playwright MCP default screenshot drop — we organize captures under
# playwright-tests/<test-slug>/screenshots/ instead. Anything that lands here
# is unsorted/stray; ignore the whole dir.
.playwright-mcp/
